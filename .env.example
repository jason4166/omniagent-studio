# Reference names only. Supply values to the operator process; do not commit secrets.
# scripts/ops.py creates installation-specific database and initial account credentials.
DEEPSEEK_API_KEY=
ZHIPUAI_API_KEY=
OMNIAGENT_WEB_PORT=8080
# For an actual website, pass --environment production --origin https://YOUR_DOMAIN.
# The operator creates .local/deployments/<project>/ and pins the environment/origin.
