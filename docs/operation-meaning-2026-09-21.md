# 操作追问：查看位置、数据存储与业务生效

运行应用源码：`ccaa3cb4c0a803607001f2c96731212f1ee112a9`。这是[额度与操作追问修复](quota-and-followups-2026-09-21.md)之后的补充，保留前一版记录。

用户反馈两种答偏：问“数据保存在哪”仍得到打开执行记录的指引，问“怎么让折扣生效”则得到通用政策。前一版只区分结果、页面位置和下一步，没有表达存储与实际业务能力之间的区别。

本次将这两类问题纳入语义路由：平台存储说明来自服务端持久化实现；业务生效说明来自注册工具的受信适配器契约。只有匹配预批准工具及适配器的注册过程才能绑定对应能力说明，不能仅凭同名工具或文档内容套用内置能力。未知契约不推断业务效果。仍从当前用户的当前会话解析操作引用，校验 Profile、工具及角色；回答追问不重复执行工具。

创建成功说明还要求成功状态、匹配的工具名称、非空记录编号及 `status=created` 回执。完成一次工具调用不自动等于业务目标已经实现。

## 当前事实

- 申请参数保存在 PostgreSQL `approvals.arguments`，会话、事件和请求快照另有相应记录。`mock_effects` 保存幂等执行回执与参数哈希，不能将其描述成保存全部申请详情的业务表。
- 本站 `request_discount` 仅创建申请记录，不修改报价、订单或商品价格，也没有第二级业务审批或启用入口。“批准”授权的是创建记录。
- 详细会话与审批信息按 TTL 清理，不能承诺永久保留。保留的回执哈希不能还原已清理的完整参数。

本次没有新增折扣生效、真实 CRM 或订单系统接入，也没有更改业务写入、幂等、权限或额度策略。仅修正助手对既有能力的理解及说明。

## 部署范围

应用从固定提交构建，四个镜像使用一致版本标签；保留现有数据库卷，沿用原迁移版本。上线前生成加密备份，保留前一版镜像用于回滚。[构建清单](artifacts/operation-meaning-20260921/build-manifest.json)与[部署记录](artifacts/operation-meaning-20260921/deployment.json)保存对应版本和健康状态。

按用户要求，没有运行自动测试、模型评测或浏览器试聊。完成 Python 静态类型检查、应用导入、镜像构建以及部署所需健康检查；回归测试代码已补充但未执行。不能据此宣称自然语言问法已全部通过验收。

代码已创建本地提交并部署；本次未执行 Git push。原申请继续保留，无需再次提交。
