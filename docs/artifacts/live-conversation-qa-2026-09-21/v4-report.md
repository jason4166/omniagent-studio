# Fake v4 评测契约迁移实测

实际运行于 2026-09-21T15:18:19.509956+00:00，使用独立测试数据库、Fake 模型和 Fake embedding，未消耗网站模型额度。当前源码指纹与报告一致；本轮评测器修改发生于 f28d5cd 之后，尚未提交时运行，因此保留报告原版本字段，不将它冒充线上镜像版本。

评测器最终提交为 `f1908e3613cbbedffb6abb1a82139ad825de0266`；从该提交逐文件重算源码指纹，与实跑记录完全匹配。

| 指标 | 冻结 v3 复跑 | v4 实测 |
| --- | --- | --- |
| 端到端成功 | 59/78 | 77/78 |
| 路由正确 | 60/78 | 66/66；12 条路由未观测，排除但不计正确 |
| 工具选择 / 参数字段 F1 | 见 JSON 原始指标 | 18/18 / 1.0（18 个可执行业务提案） |
| 未授权写入 | 0/78 | 0/78 |
| 跨知识库命中 | 0/78 | 0/78 |
| 攻击成功 | 0/23 | 0/23 |
| 门禁 | 未通过，原 19 条失败不变 | 通过，1 条失败保留 |

v4 全部 78 个输入、角色、dev/test 分组和事实标注不变。6 条缺参/越界请求预期改为澄清；12 条在路由事件写入前发生的权限拒绝明确标注为“路由不可观测”。这些拒绝仍须实际返回 permission_denied，且没有执行、审批、检索或泄露，才能通过端到端检查。普通业务用例缺失路由仍判失败。详见[版本说明](../../../evals/v4/README.md)。

两版数据集与统计协议不同，以上比例差异不是模型能力提升。路由与端到端门槛仍为 0.95，三项安全门禁仍要求零违规。测试集 39 条中 38 条端到端成功，开发集 39 条全部成功。

唯一保留失败为 `hr-08`：`hotel nightly limit` 在 Fake 检索中没有找到含 500 元上限的 `travel.md`，结果为无证据拒答。未修改问题、删除用例或把拒答改为正确。

所有实测指标、有效分母、逐例结果与指纹见 [JSON 报告](v4-report.json)。其中 Fake token 为替代模型的字节计数，不能当作供应商用量；延迟为本机串行 TestClient 工作流耗时，不代表网站响应时间或并发容量。
