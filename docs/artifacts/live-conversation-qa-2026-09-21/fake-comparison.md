# Fake 评测基线对比（2026-09-21）

本轮新增失败 **0**；原有失败 **19**。冻结门禁在基线与候选上均未通过，不能把这一结果表述为评测通过。两次评测的未授权写入、知识库隔离违规和攻击成功率均为 0。

| 项目 | 基线 fd3ec46 | 候选 0155adb 对应源码 |
| --- | --- | --- |
| 用例数 | 78 | 78 |
| 路由正确 | 60/78（76.9231%） | 60/78（76.9231%） |
| 端到端成功 | 59/78（75.6410%） | 59/78（75.6410%） |
| 原有失败 | 19 | 同一组 19 |
| 新增失败 | — | 0 |
| 实际模型 / embedding API 调用 | 0 / 0 | 0 / 0 |

冻结数据集：`omniagent-e2e-v3-conversation-boundary`。SHA-256：`d5e82673cd56edcdc17e52cdbf2e4dbffde3da8cb3e45e55f522360c6e040422`。

先在全量测试使用过的临时数据库运行候选，再在独立空库 `liveqa_eval` 迁移并复跑，结果逐例一致，排除了测试残留。基线来自 `git archive fd3ec46`，在另一独立空库 `liveqa_eval_head` 迁移后运行同一数据集；78 个用例的路由、结果、安全标志与候选逐项一致。全程使用 Fake provider 与 Fake embedding，没有真实模型调用。

## 19 项既有失败的原因

- **12 项权限拒绝的路由未记录**：授权检查在 `route.selected` 事件写入前拒绝请求；评测器读到空路由，而冻结预期是 `tool`。实际结果仍为拒绝，没有不安全执行。
- **6 项参数校验契约与旧预期不同**：缺参、越界或额外字段现在返回 `clarify`，冻结数据仍预期 `tool/denied`。澄清没有执行工具，也不是安全失败。
- **1 项 Fake 检索失败（hr-08）**：`hotel nightly limit` 未在前 5 个候选中找到包含 500 元住宿上限的 `travel.md`。基线与候选都拒答；原有应回答预期保持不变。

## 源码归属证据

候选评测发生在提交 0155adb 之前，因此原始报告中的 `git_commit` 仍为 `fd3ec46dc80337e3d948c559a555a5ea7c76d920`，没有改写。报告记录的源码版本为 `local:8acbf48e04fb971bb51e77fce8d74a535e6c7e2e76d340c494d1c0f8e9a50d7f`。从提交 `0155adbaab55c340bce171ed0c7d40b6c1771e4e` 逐文件重算 `src/omniagent/*.py` 的同算法指纹，结果完全匹配：`8acbf48e04fb971bb51e77fce8d74a535e6c7e2e76d340c494d1c0f8e9a50d7f`。

基线完整提交为 `fd3ec46dc80337e3d948c559a555a5ea7c76d920`，源码指纹为 `581335d42a9afc86263b3d00554e8c5d0a93edc6c40f690ee82350c045ef7b33`。上述指纹覆盖直接 Python 模块并统一换行，不冒充完整容器或所有资源文件的指纹。原始评测版本、用例清单及分组保存在 [脱敏 JSON 对比](fake-comparison.json)；此文件不包含原始会话、cookie、凭据或完整 trace。

冻结用例、评分协议与门槛均未修改。本对比仅确认本次没有新增 Fake 工作流失败，真实对话质量需由另行执行的真实对话回归确认。
