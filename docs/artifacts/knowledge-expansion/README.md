# 访问入口与知识扩展验证

应用变更见 `68b23aa`、`9e46785`；连续聊天脚本修正见 `77ad5d1`。[机器可读记录](verification.json) 从本次 JUnit、覆盖率、评测和 HTTP 验收报告提取，保留数据集及测试代码标识。

- 后端：924 项通过，行覆盖率 89.75%；Ruff check、format check、mypy 与 diff 检查通过。
- 浏览器：9 项通过，覆盖预填登录、只读配置、访客隔离、引用、HTTP/MCP、审批编辑及重放。
- 安全：166 项通过；源文件及 Git 可达历史扫描未发现密钥。
- 原有真实模型评测：37/37；新增自然问答：24/24，新增集 Recall@1 为 23/24、Recall@3/5 为 24/24。这里是固定用例结果，不是通用回答准确率。
- Fake 回归：77/78，通过现有门禁；保留旧用例 `hr-08` 对英文 `hotel nightly limit` 的拒答失败。新增集不使用 Fake 随机向量衡量中文语义质量。
- 同一账号与独立公开访客各完成一轮 18 次输入的多轮验收，重复请求未增加模型或工具用量；另测两条公开访客自然问法，均回答并引用原文。
- 当前预置资料：HR 11 份、产品支持 9 份、销售 9 份，共 57 个内容单元；重复 seed 前后的来源与索引身份完全一致。

首次聊天验收曾因要求出现“需求”二字而误判一条已列出采购范围、预算和时间的正确回答，后改为检查具体的预算信息。额外重复验收触发了同账号日额度，随后只补偿有报告记录的本轮测试个人额度，并保留全站实际用量与审计，再使用独立访客完成验收。后端运行另有一条 Windows pytest 缓存目录权限警告，不影响测试结果；CI 已禁用该可选缓存。

真实部署更新了已有本地服务，未新建公网站点；独立 Fake 环境先在新卷完成启动，再更新到扩展资料版本。模型价格未知仍报告 `unknown`。复现命令与内容范围见 [知识内容说明](../../knowledge-content.md) 和 [新数据集](../../../evals/knowledge-v1/README.md)。
