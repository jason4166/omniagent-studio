# Milestone E verified delivery

Tested source: `a8ce87ff063c64d6db5f3e053b2e70411a422d73`. [Release evidence](release/README.md) contains the generated gate totals, immutable report identities, image IDs, coverage and artifact hashes.

The final run used a fresh clone, venv, node_modules and PostgreSQL volume. It passed Compose build, migration, repeatable seed, health/readiness, a full non-root Linux backend gate, native static checks, frontend lint/typecheck/unit/build, Chromium E2E, dedicated security/history/image scans, frozen evaluation and a separate benchmark. Two consecutive actual paced demonstrations passed in 182.141 and 182.109 seconds.

Acceptance covers HR citations and refusal, HTTP/MCP read tools, durable sales interrupt, PostgreSQL/mock/API restart, edited approval, repeated approval with one effect, rejected discount with no effect, and SSE disconnect/cursor replay without another write. Screenshots were generated by the same clean browser run and reviewed.

Container validation exposed and fixed nested cache inclusion, Windows checkout line-ending drift, stale proxy DNS after restart and CI-user coverage permissions. Earlier UI verification fixed lost historical citations and edits overwritten by SSE refresh. These failures remain documented in the failure record and ADR 0009.

README, architecture/state/approval/data diagrams, nine ADRs, threat model, MIT/data licenses, operations/development instructions, changelog, release notes and the three-minute demo script are public release inputs. Required Fake CI and manual-only real-provider CI are defined with pinned actions and lockfile caches. Remote CI was not triggered and no remote publication occurred.

One held-out Fake query safely abstains; one optional real-provider answer is rejected by strict support validation. Neither failure is hidden or removed from reports. The local Fake/mock candidate passes all required gates; the optional real-model quality result remains 2/3.
